/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseBlocageResponse } from '../../models/page-response-blocage-response';

export interface SearchBlocages$Params {
  page?: number;
  size?: number;
  titre?: string;
  statut?: 'EN_COURS' | 'EN_ATTENTE' | 'RESOLU';
  priorite?: number;
}

export function searchBlocages(http: HttpClient, rootUrl: string, params?: SearchBlocages$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseBlocageResponse>> {
  const rb = new RequestBuilder(rootUrl, searchBlocages.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
    rb.query('titre', params.titre, {});
    rb.query('statut', params.statut, {});
    rb.query('priorite', params.priorite, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseBlocageResponse>;
    })
  );
}

searchBlocages.PATH = '/blocages/search';
